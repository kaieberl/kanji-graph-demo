# Query Wrapper

`query_graph.py` is a compatibility wrapper for the original graph-query experiment. It delegates to `src/main.py`, which now contains the maintained implementation.

```bash
python kanji_graph/query_graph.py --component 寺
```

![Kanji containing 寺](../docs/figures/component_寺.png)
